services:
  web:
    build: .
    expose:
      - "80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz.json"]
      interval: 30s
      timeout: 5s
      start_period: 5s
      retries: 3
